<div class="modal fade right" id="withdrawModal" tabindex="-1" aria-labelledby="withdrawModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="withdrawModalLabel">Retirer de l'argent</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="form-group mt-3">
                    <form method="post" action="{{ path('user_item', {'id': user.id}) }}">
                        {{ form_label(withdrawForm.amount) }}
                        {{ form_widget(withdrawForm.amount, {'attr': {'class': 'form-control'}}) }}
                        {% if form_errors(withdrawForm.amount) %}
                            <div class="invalid-message text-danger">
                                {{ form_errors(withdrawForm.amount) }}
                            </div>
                        {% endif %}

                        {{ form_label(withdrawForm.bankAccount) }}
                        {{ form_widget(withdrawForm.bankAccount, {'attr': {'class': 'form-control'}}) }}
                        {% if form_errors(withdrawForm.bankAccount) %}
                            <div class="invalid-message text-danger">
                                {{ form_errors(withdrawForm.bankAccount) }}
                            </div>
                        {% endif %}

                        {{ form_widget(withdrawForm.submit, {'attr': {'class': 'btn pi-bg text-white mt-3'}}) }}

                        <input type="hidden" name="_csrf_token_transaction_user" value="{{ csrf_token('user_withdraw_form') }}">
                        <input type="hidden" name="form_withdraw" value="1">
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>